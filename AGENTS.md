# AGENTS.md

## 文件定位

- 本文件是本项目面向 AI Agent 的唯一事实来源（single source of truth）。
- 任何 Agent 开始工作前，必须先完整阅读本文件和 `README.md`，再检查 `git status` 与最近提交。
- 不要依赖本机绝对路径。所有命令和文件引用应以仓库根目录为基准，保证 Windows、macOS 和 Linux 上可迁移。
- 若某个平台不会自动读取 `AGENTS.md`，应在该平台的项目指令中明确要求“先读取并遵守仓库根目录的 `AGENTS.md`”，不要复制出一份长期分叉的规则。

## 项目目标

“片库归档台”是一个 local-first 的浏览器/服务器（B/S）工具，用于整理本地视频文件夹：

- 从带广告、后缀混乱的文件名中识别和规范化番号。
- 在执行前预览批量重命名、广告隔离和演员归档结果，并允许人工修正。
- 保存人工修正规则，可选择是否回溯应用当前批次。
- 刮削并保存影片标题、演员、片商、封面、NFO 和本地 JSON 元数据。
- 将多个来源目录归集到指定输出目录，支持复制、移动和同卷硬链接。
- 按番号归组，并以“同番号 + 同大小候选 + SHA-256”识别完全重复内容。

项目只监听 `127.0.0.1:4318`，定位是单机工具，不应默认开放到局域网或公网。

## 用户与沟通

- 默认使用中文沟通、编写用户可见文案和错误信息；代码标识、命令和配置键保持英文。
- 用户是后端开发，正在学习 AI、Agent、MCP 和自动化开发。涉及模型、提示词、工具调用或 Agent 设计时，需要解释关键原理、执行过程和方案取舍。
- 创意细节不影响核心结果时可以合理假设并直接实施；缺失信息会改变文件处理结果或扩大数据风险时，先询问用户。
- 安装依赖、工具或 Agent 扩展前，必须先说明来源、发布者、仓库地址、安装位置和用途，并得到用户确认。

## 技术架构

- 运行时：Node.js 20+，ES Modules。
- 依赖策略：当前没有第三方 npm 运行依赖或开发依赖，优先使用 Node.js 标准库。
- 后端入口：`src/server.mjs`，负责本地 HTTP、JSON API 和静态资源服务。
- 前端：`public/index.html`、`public/app.js`、`public/styles.css`，使用原生 HTML/CSS/JavaScript，无构建步骤。
- 核心服务：
  - `src/lib/filename-parser.mjs`：广告清理、番号识别、模板渲染。
  - `src/lib/file-scanner.mjs`：递归扫描、视频/字幕配对、元数据读取。
  - `src/lib/rename-service.mjs`：重命名、广告隔离、演员目录移动的预览与执行。
  - `src/lib/library-service.mjs`：多来源片库索引、内容哈希、复制/移动/硬链接归集。
  - `src/lib/settings-service.mjs`：命名模板、广告关键词和修正规则。
  - `src/lib/scraper-service.mjs`：多刮削源注册、站点解析策略和自动故障转移。
  - `src/lib/metadata-service.mjs`：NFO、JSON 和封面保存。
  - `src/lib/scrapers/javbus.mjs`：JavBus HTML 适配器；JavDB、JavLibrary 和通用页面适配由 scraper service 统一编排。
- 测试：`test/` 使用 Node.js 内置 `node:test`，统一入口为 `test/all.test.mjs`。
- 演示数据：`demo-library/` 中的媒体文件都是极小文本占位文件，不是真实视频。

## 常用命令

在仓库根目录运行：

```sh
npm test
npm start
```

Windows 也可以双击 `start.cmd`，或执行：

```powershell
npm.cmd test
node src/server.mjs
```

项目没有安装步骤。除非实现确实需要新的第三方依赖，否则不要运行 `npm install` 或引入构建工具。

## 数据与安全边界

- `.media-archiver/` 保存本机配置，`.media-archiver-trash/` 保存隔离杂质；两者都是运行数据，不得提交。
- 不得提交真实视频、字幕、封面、用户目录清单、账号、Cookie、Token、凭据或包含隐私的刮削结果。`demo-library/` 的占位文件是唯一例外。
- 不得把测试指向用户真实片库。涉及文件修改的测试必须使用临时目录或演示副本，并在测试后清理。
- 所有来自 UI/API 的路径都视为不可信输入。必须规范化路径、防止目录穿越，并验证目标仍位于允许的根目录中。
- 不得静默覆盖已有文件，不得以“去重”为理由自动删除源文件。
- 网络刮削失败必须返回可理解的错误，且不能阻塞纯本地扫描和整理功能。

## 不可破坏的业务规则

1. 任何重命名、移动、复制、硬链接、隔离或元数据写入，都必须先提供预览或明确的用户确认。
2. 预览中的错误项不能阻止其他安全项执行；执行接口应支持跳过错误项，并逐项返回结果。
3. 广告杂质默认移动到 `.media-archiver-trash/`，不做永久删除。
4. 重命名和归集不能覆盖现有目标；部分失败时应尽量回滚或保持源文件可用。
5. 按演员归档必须有可靠的演员信息，并移动完整影片文件夹；缺演员、部分选择或目标冲突时必须阻止该项。
6. 完全重复只标记和跳过，不自动删除。内容不同的同番号文件应保留为 `-V2` 等变体。
7. 只有同一磁盘卷才允许硬链接。跨卷移动必须先复制并验证成功，再删除源文件。
8. 输出目录位于来源目录内部时，后续索引必须排除输出目录，避免递归归集。
9. 文件名识别保持保守；低置信度和未识别项目交给人工确认，不要扩大正则导致误判普通文件。
10. 长时间任务必须有可回看的进度和逐项日志，日志应说明当前对象、动作、成功或失败原因。

## 开发约定

- 先理解现有模块和测试，再修改代码；保持改动聚焦，不做与需求无关的重构。
- 优先扩展现有服务和数据结构，不在前端重复实现已经存在的后端业务规则。
- 新增或修改 API 时，同步处理输入校验、中文错误信息、前端忙碌/失败状态和自动化测试。
- 涉及文件系统行为时，至少覆盖：正常路径、已有目标、非法目标、部分错误、跨卷/同卷差异和回滚或保留源文件行为。
- 修改番号解析、广告规则或命名模板时，必须增加针对真实命名形态的表驱动测试，并保留原有案例。
- 日期型无码番号以 MDC、JavSP、Javinizer-Go 等主流开源工具的规则交叉校验：1Pondo/Pacopacomama 使用 6+3 位、10Musume 使用 6+2 位、Caribbeancom 使用 6+3 位。厂商前后缀属于兼容输入，不直接视为规范输出；不满足真实位数的猜测值必须交给人工确认。
- 修改用户可见功能后同步更新 `README.md`；如果架构、安全边界或接手流程变化，同步更新本文件。
- 保留用户已有改动。发现无关的未提交文件（例如个人 IDE 配置）时不要擅自暂存、删除或覆盖。

## 验证与完成标准

当前测试基线为 36 项。每次交付至少执行：

```sh
npm test
git status --short
```

涉及页面交互时，还应启动本地服务并验证受影响流程。涉及真实文件动作时，先用 `demo-library/` 或临时副本验证预览，再验证执行结果和日志。

任务完成需要满足：

- 用户请求的行为已实现，不只完成界面占位。
- 文件操作仍遵守上述安全规则。
- 相关测试通过，并说明未能执行的检查。
- 没有把运行数据、真实媒体或无关本地文件带入提交。
- 最终交接说明包含改动、验证结果、已知限制和下一步状态。

## 当前已知限制

- JavBus、JavDB 和 JavLibrary 使用网页结构适配，不是官方 API；可能受网络、反爬和页面改版影响。
- 当前工具面向单机和单用户，不包含身份认证、并发任务队列或远程访问能力。
- 番号识别规则有意保持保守，无法识别的命名需要人工修正。
